services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: survey
      POSTGRES_PASSWORD: survey
      POSTGRES_DB: survey_builder
    ports:
      - "5432:5432"
    volumes:
      - survey_builder_pgdata:/var/lib/postgresql/data

volumes:
  survey_builder_pgdata:
